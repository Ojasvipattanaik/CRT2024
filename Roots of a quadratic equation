#include <stdio.h>
#include<math.h>
int main() {
    float a,b,c;
    scanf("%f%f%f",&a,&b,&c);
    float d = b*b-(2*a*c);
    if(d<0){
        printf("Roots are imaginary");
    }
    else{
        printf("%f",(-b+sqrt(d))/2*a);
        printf("%f",(-b-sqrt(d))/2*a);
    }
    return 0;
}
